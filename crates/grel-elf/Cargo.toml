[package]
name = "grel-elf"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "ELF system dependency auto-resolution for grel (Linux-only)"

[dependencies]
grel-cache = { path = "../grel-cache" }
grel-config = { path = "../grel-config" }
grel-network = { path = "../grel-network" }

goblin.workspace = true
tokio.workspace = true
tracing.workspace = true
thiserror.workspace = true
anyhow.workspace = true

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
